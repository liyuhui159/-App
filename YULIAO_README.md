# 语聊（Yuliao）

语聊是一个独立的原生 Android 聊天回复助手模块，位于 `yuliao/`，不会修改现有的“灵犀记账”应用逻辑。

## 双模式功能

### 离线语录

- 默认启动，无需联网、无需 API Key
- 内置 500+ 条经过人工筛选、纠错、改写和去重的基础语录
- 覆盖约 40 类聊天意图，包括问候、状态、邀约、夸奖、感谢、道歉、想念、晚安、忙碌、冷场、失恋、争执、求助、祝贺、旅行、影视音乐、购物、游戏、健康等
- 结合 6 种聊天场景、6 种回复人设以及回复长度规则，可形成 20,000+ 种候选组合
- 支持短句、正常、一段话以及 4/6/8 条候选回复
- 断网状态下完成关键词匹配、意图排序、人设改写和场景组合

### 在线 AI

- AI 分析对方原话的语气、情绪、关系距离和潜台词
- 根据场景、人设、回复长度、数量和自定义补充要求实时生成
- 支持 OpenAI Responses API，并兼容常见 Chat Completions 接口
- API 地址、API Key 和模型名称均可在手机端配置

## 通用功能

- 粘贴剪贴板中的一句话或一段聊天内容
- 从微信、QQ、短信等应用通过“分享文字”打开语聊
- 选中文字后通过 Android 的 `PROCESS_TEXT` 菜单调用语聊
- 场景：日常聊天、暧昧互动、朋友互怼、破冰开场、缓和气氛、职场沟通
- 人设：幽默风趣、高情商温柔、轻松俏皮、克制礼貌、毒舌但不伤人、真诚直接
- 单条复制、系统分享、离线结果转在线继续优化

## 构建

```bash
./gradlew :yuliao:assembleDebug
```

APK 输出位置：

```text
yuliao/build/outputs/apk/debug/yuliao-debug.apk
```

也可以在 GitHub Actions 页面手动运行 `Build Yuliao APK`，完成后下载 `yuliao-debug-apk` 构建产物。

## 使用方法

1. 安装并打开“语聊”，默认进入“离线语录”。
2. 粘贴聊天内容，选择场景、人设、长度和数量。
3. 点击“从离线语录匹配回复”，断网也能使用。
4. 需要更准确的上下文分析时，切换到“在线 AI”。
5. 第一次使用在线模式，点击右上角“API 设置”，填写 API 地址、API Key 和模型名称。
6. 默认 API 地址为 `https://api.openai.com/v1/responses`，默认模型为 `gpt-5-mini`。

## API Key 安全说明

API Key 不会写入代码或提交到 GitHub，只保存在当前设备的 App 私有数据中。该方式适合个人测试。若未来公开发布或多人使用，应增加自有后端，由服务器保管 API Key，App 只调用你的业务接口。

## 离线语料与许可证

- 少量中文基础问答经过筛选和改写后取自 BSD-3-Clause 许可的 ChatterBot Corpus。
- LCCC 用于参考开放域中文对话的意图分布和清洗方式，不在 APK 中直接再分发其大规模社交媒体原文。
- CPED 用于参考情绪、对话行为、人格和场景分类，不在 APK 中直接再分发电视剧原始台词。
- 其余大部分回复由本项目重新编写、清洗和组合。
- 已过滤广告、联系方式、隐私信息、人身攻击、色情低俗、违法引导、明显机翻、过时网络梗和上下文残缺内容。

详细说明见：

- `YULIAO_OFFLINE_SOURCES.md`
- `yuliao/src/main/assets/OFFLINE_CORPUS_NOTICES.txt`

## 后续建议

- 增加用户自定义离线语录包导入和导出
- 增加收藏夹和历史记录
- 增加自定义人设库
- 增加悬浮窗快捷入口
- 增加截图识别聊天内容
- 增加服务端账号体系和会员额度
